def find_motif_positions(sequence: str, motif: str) -> list:
    """
    Returns a list of start indices where `motif` occurs in `sequence`.
    """
    # your algorithm here (start with naive search!)
    return []
